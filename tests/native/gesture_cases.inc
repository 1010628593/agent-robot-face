/* GENERATED by tools/gen_gesture_cases.py from
 * Bot_Status_v1_Handoff/acceptance/gesture_cases.json — do not edit by hand. */

static const gesture_sample_t samples_G01[] = {
    { BOT_TOUCH_DOWN, 0u, 233, 233 },
    { BOT_TOUCH_UP, 100u, 233, 233 },
};
static const bot_gesture_kind_t expected_G01[] = { BOT_GESTURE_TAP };

static const gesture_sample_t samples_G02[] = {
    { BOT_TOUCH_DOWN, 0u, 233, 233 },
    { BOT_TOUCH_TICK, 649u, 233, 233 },
    { BOT_TOUCH_UP, 649u, 233, 233 },
};
static const bot_gesture_kind_t expected_G02[1] = { BOT_GESTURE_NONE };

static const gesture_sample_t samples_G03[] = {
    { BOT_TOUCH_DOWN, 0u, 233, 233 },
    { BOT_TOUCH_TICK, 650u, 233, 233 },
    { BOT_TOUCH_UP, 700u, 233, 233 },
};
static const bot_gesture_kind_t expected_G03[] = { BOT_GESTURE_HOLD };

static const gesture_sample_t samples_G04[] = {
    { BOT_TOUCH_DOWN, 0u, 233, 233 },
    { BOT_TOUCH_MOVE, 300u, 246, 233 },
    { BOT_TOUCH_TICK, 650u, 246, 233 },
    { BOT_TOUCH_UP, 700u, 246, 233 },
};
static const bot_gesture_kind_t expected_G04[1] = { BOT_GESTURE_NONE };

static const gesture_sample_t samples_G05[] = {
    { BOT_TOUCH_DOWN, 0u, 300, 233 },
    { BOT_TOUCH_MOVE, 150u, 235, 233 },
    { BOT_TOUCH_UP, 250u, 210, 233 },
};
static const bot_gesture_kind_t expected_G05[] = { BOT_GESTURE_SWIPE_LEFT };

static const gesture_sample_t samples_G06[] = {
    { BOT_TOUCH_DOWN, 0u, 180, 233 },
    { BOT_TOUCH_MOVE, 150u, 246, 233 },
    { BOT_TOUCH_UP, 250u, 270, 233 },
};
static const bot_gesture_kind_t expected_G06[] = { BOT_GESTURE_SWIPE_RIGHT };

static const gesture_sample_t samples_G07[] = {
    { BOT_TOUCH_DOWN, 0u, 233, 300 },
    { BOT_TOUCH_MOVE, 150u, 233, 235 },
    { BOT_TOUCH_UP, 250u, 233, 210 },
};
static const bot_gesture_kind_t expected_G07[] = { BOT_GESTURE_SWIPE_UP };

static const gesture_sample_t samples_G08[] = {
    { BOT_TOUCH_DOWN, 0u, 233, 170 },
    { BOT_TOUCH_MOVE, 150u, 233, 238 },
    { BOT_TOUCH_UP, 250u, 233, 270 },
};
static const bot_gesture_kind_t expected_G08[] = { BOT_GESTURE_SWIPE_DOWN };

static const gesture_sample_t samples_G09[] = {
    { BOT_TOUCH_DOWN, 0u, 233, 233 },
    { BOT_TOUCH_MOVE, 150u, 300, 300 },
    { BOT_TOUCH_UP, 250u, 310, 310 },
};
static const bot_gesture_kind_t expected_G09[1] = { BOT_GESTURE_NONE };

static const gesture_sample_t samples_G10[] = {
    { BOT_TOUCH_DOWN, 0u, 233, 233 },
    { BOT_TOUCH_TICK, 650u, 233, 233 },
    { BOT_TOUCH_UP, 700u, 233, 233 },
};
static const bot_gesture_kind_t expected_G10[] = { BOT_GESTURE_WAKE_ONLY };

static const gesture_sample_t samples_G11[] = {
    { BOT_TOUCH_DOWN, 0u, 233, 233 },
    { BOT_TOUCH_CANCEL, 100u, 233, 233 },
    { BOT_TOUCH_TICK, 700u, 233, 233 },
};
static const bot_gesture_kind_t expected_G11[1] = { BOT_GESTURE_NONE };

static const gesture_sample_t samples_G12[] = {
    { BOT_TOUCH_DOWN, 0u, 300, 233 },
    { BOT_TOUCH_MOVE, 200u, 245, 233 },
    { BOT_TOUCH_UP, 300u, 245, 233 },
};
static const bot_gesture_kind_t expected_G12[1] = { BOT_GESTURE_NONE };

static const gesture_case_t GESTURE_CASES[] = {
    { "G01", samples_G01, 2u, expected_G01, 1u, false },
    { "G02", samples_G02, 3u, expected_G02, 0u, false },
    { "G03", samples_G03, 3u, expected_G03, 1u, false },
    { "G04", samples_G04, 4u, expected_G04, 0u, false },
    { "G05", samples_G05, 3u, expected_G05, 1u, false },
    { "G06", samples_G06, 3u, expected_G06, 1u, false },
    { "G07", samples_G07, 3u, expected_G07, 1u, false },
    { "G08", samples_G08, 3u, expected_G08, 1u, false },
    { "G09", samples_G09, 3u, expected_G09, 0u, false },
    { "G10", samples_G10, 3u, expected_G10, 1u, true },
    { "G11", samples_G11, 3u, expected_G11, 0u, false },
    { "G12", samples_G12, 3u, expected_G12, 0u, false },
};
static const uint32_t GESTURE_CASE_COUNT = 12u; /* samples total: 36 */
