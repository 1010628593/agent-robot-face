# Approved formal-product specification
Authority: user-approved implementation request in current Codex task, 2026-09-07.

Four observed sources: Codex Desktop+CLI, Cursor IDE, Hermes CLI, WorkBuddy Desktop. No reported/manual/CodeBuddy substitution for WorkBuddy. Failed source stays explicitly blocked; rest proceeds. Source facts need real task evidence, not templates.

Constant #C9DCFF opaque eyes on black, black pupils always in normal open eyes. Blink/closed smile/error topology has documented occlusion, no state-dependent pupil fading, no brightness animation. Idle playful but restrained; work restrained; important states suppress playful overlays. Preserve 98.7-degree IMU alignment and 20ms sampling/dirty-region rendering.

Central radius190 owns petting; outer ring owns horizontal page swipe. Ownership latched at down. Hold650ms/movement13px retained; stroke path40px and two meaningful reversals. Tap actions wink/surprise/curiosity/smirk; stroke actions contented/rub/playful shake. Shuffle selection no repeat, duration600-1500ms, cooldown400ms, no backlog. Important state cancels within120ms. No pose-driven semantic state writes.

Production no SIM cycling or fake data. Explicit independent developer simulator only. Device Face, Picker, information Task/Today/Quota tabs, real source health/missing/stale/offline/ack feedback, monochrome recognizable product icons and subset Chinese fonts. Chinese UI, original product names. Mac native menu bar controls status, focus auto/pinned, sources, diagnostics, launch-at-login and pause/resume. No full desktop management window or task control/approval.

Python isolated Bridge, SQLite ledger, user LaunchAgent, minimal merge hooks preserving other integrations. Whitelist sanitized metadata only, no raw prompt/command/message/credentials. v2 @bot framing, bounded queues, single TX owner, link/seq/revision/dedup. Official selection atomically carries mode+agent+revision. Auto: waiting then unresolved error then retain active then recent active; manual pins agent. Missing metrics null with provenance, never fake zeros/quotas. USB reconnect and pause for flash.

No added unit tests. Existing checks, protocol/integration/real LVGL rendering and hardware validation. Each batch firmware modifications build+flash+log within existing authorization. No claim four-source complete until each real lifecycle passed. No reset credit/account mutations.
