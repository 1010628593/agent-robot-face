# 2026-09-06

## Bot Status v1 · 启动（T00 + T02 构建）

- 按 `Bot_Status_v1_Handoff` 文档包实施。已读 AGENTS/README/01_PRD/02_UI_UX/03_STATE_MODEL/05_PROTOCOL/10_IMPLEMENTATION_PLAN/START_HERE_macOS。
- 代码根定为 `/Users/hanxin/Workspace/llm/agent-robot-face`（用户要求按当前目录）；文档包建议的 `~/Workspace/bot-status` 当时不存在。
- Git 仓库初始化并提交 2 次：`d50ceaf`（T00+T02 证据）、`68e5a28`（纳入规格包）。

### 环境事实（本机实测）
- EIM ESP-IDF **v6.1**：`IDF_PATH=/Users/hanxin/.espressif/v6.1/esp-idf`，HEAD `fff9895c…`，IDF Python 3.14.7，esptool 5.4.0。
- **EIM 激活脚本缺 cmake**：cmake 装在 `tools/cmake/4.0.3/CMake.app/Contents/bin`，静态 PATH 不含；`idf_tools.py export` 因 esp-rom-elfs 版本不匹配整体报错。修补方案：项目内 `tools/idf-env.sh`（激活后补入 EIM 自带 cmake 4.0.3），不改 EIM、不降级。
- 构建时沙箱 shim 对已存在的 `build/log` 目录抛 `PermissionError` → 清掉该目录、必要时非沙箱模式重跑。

### T02 构建结果
- 官方 `examples/esp-idf/02_lvgl_demo_v9` 用 v6.1 **编译通过**（2160/2160），`lvgl_demo_v9.bin` 967,696 B。
- 锁定依赖：BSP `waveshare/esp32_s3_touch_amoled_1.75 3.0.1`、LVGL `9.4.0`、`esp_lcd_co5300 2.1.0`、`waveshare/esp_lcd_touch_cst9217 2.0.0`、idf 6.1.0。BSP 3.0.1 对 IDF>=6.0 条件依赖 `espressif/usb`，说明 v6.1 在支持范围。
- vendor BSP 克隆 HEAD `e4344e70`（2026-08-21），`vendor/` 已 gitignore（嵌套克隆，靠 SHA 固定）。

### 硬件已接入（20:22 之后）
- 端口 **`/dev/cu.usbmodem2142101`**（原生 USB CDC，插拔对比确认）。`/dev/cu.usbserial-214220` 是 ASUS 外设，别认错。
- flash-id：ESP32-S3 (QFN56) rev v0.2、16MB flash、8MB PSRAM (AP_3v3)、USB-Serial/JTAG、MAC 28:84:85:8d:71:e0。
- eFuse 只读检查：Secure Boot 关、Flash 加密关、WR_DIS/RD_DIS=0、JTAG 未禁用 → 备份明文可用。
- **原厂备份已完成**：`$HOME/Workspace/bot-status-private-backups/20260906-202608/factory-full.bin`，16,777,216 B，SHA256 `4c020428c2fe5c7610a4694682ddc473427707ad553a21afe168c090d0b33c38`（仓库外，umask 077）。
- **重要差异**：原厂分区表 `factory@0x110000`（另有 ota_0/assets/storage），官方示例 `factory@0x10000(8M)+storage@0x810000(7M)`。烧示例会覆盖分区表使原厂分区成孤儿；**恢复必须整片回写备份，不能只写 app**。
- 状态：**已烧录（用户确认"烧录 + monitor"）**。

### 烧录与实机验证（20:5x）
- 用构建产物 `build-idf61/flash_args`（`--flash-mode dio --flash-freq 80m --flash-size 16MB`）直调 esptool 写入 3 分区；967,696 B @0x10000，hash verified；**未 erase-all、未写 eFuse**。
- 坑：受管环境里 `idf.py flash/monitor` 会撞 `os.mkdir('build/log')` 已存在的 shim 报错（`rm -rf` 也删不掉）。绕法：直接用 esptool + flash_args；抓日志用 pyserial 脚本（先拉 RTS/DTR 触发复位再读）。
- 实机日志：ESP-IDF **v6.1** 跑起来了；`co5300 2.1.0` 面板创建成功；**CST9217 报 466×466**；8MB PSRAM test OK；LVGL task started；无 Panic、无复位。仅 2 条无害 warning（3Ah 被外部初始化覆盖、官方 demo 未开手势）。
- **本示例主 console 是 UART0(GPIO43/44)，USB Serial/JTAG 只是 secondary** → 日志必须在复位瞬间抓；平时端口安静是正常的。
- 15 分钟长跑观察已后台启动 → `evidence/ui/official-demo/boot-and-15min.log`。
- 仍待用户目测屏幕画面 + 触摸四方向。

### T01 预探测（只读）
- Codex：已安装，`~/.codex/` 完整（hooks.json/config.toml/version.json/app-server-control.sock），`version.json` latest 0.152.0；二进制 `~/.local/bin/codex`（不在受限 PATH）。
- WorkBuddy Desktop 5.5.3、Cursor 3.19.14；Hermes 未在 PATH 中找到。
- WorkBuddy 桌面事件源未知 = 最高风险项；必须能力探测，不能套 CodeBuddy CLI。

### 后续进展（21:00-21:18）
- 用户上传实机照片: 显示 LVGL 9 性能基准页可读(FPS 49/29/31/66), 显示链路确认
- 15 分钟稳定性抓取: 启动日志完整 + 后续 11 分钟静默, **0 复位 0 Panic**, R01 验收通过
- T01 四源 capability-report 全部 PASS schema 校验:
  - Codex 0.152.0 partial / WorkBuddy 5.5.3 blocked / Cursor 3.19.14 partial / Hermes 0.21.0 ready
  - WorkBuddy 降级为 MCP reported(quality=reported, 短过期), 需要用户接受才能退出 blocked
- Hermes hooks list 显示已有 6 个 shell-hook(AgentKeyboard + mnemon), 新接入作第 3 调用方即可
- WorkBuddy app.asar 主进程加密; unpacked/main 只有 qimei-helper.js; cli/ 是 CodeBuddy CLI 重打包
- 不逆向 WorkBuddy 主进程, 不绕过 broker; WorkBuddy 走 MCP 降级

### 待用户决定
- WorkBuddy 是否接受 reported 降级(否则 T18 维持 BLOCKED_SOURCE)

### T03 完成（21:25-21:45）
- 用户决策: WorkBuddy 选 B(维持 BLOCKED_SOURCE, 拒绝 MCP reported 降级), T18 跳过, 已提交 fd71f44
- bridge/ Python 包: models.py 三层校验(strict_load 解析器 + Pydantic 严格模型 + 语义层)
- 语义规则必须与 Bot_Status_v1_Handoff/tools/validate_contracts.py 一致(合同权威), 不一致时改 models
- 关键坑: hello 是唯一允许 link_id=null/seq=0 的消息(schema allOf[10]); AgentEvent 有 source_health 字段
- TDD 过程: 测试先行 -> 首次 ModuleNotFoundError -> 实现后 2 失败(09 link 规则 + source_health) -> 修复 20/20 PASS
- 依赖锁定: pydantic 2.13.5 / jsonschema 4.26.0 / pytest 8.4.2(bridge/requirements.lock)
- firmware C 侧: bot_types.h + tests/native/ CMake, macOS clang -Werror 编译 ctest PASS
- 验收命令: .venv-bridge/bin/python -m pytest bridge/tests/test_contracts.py -q

### T04+T05+T06-T08 完成（21:45-23:00）
- T04 手势 FSM 12/12 表驱动 PASS（commit 6179750）；工具: tools/gen_gesture_cases.py 从 JSON 生成 C 表
- T05 帧解码/Model/路由 5/5 native PASS（commit 443e942）
  - bot_json 严格解析器(固定池零堆)；bot_frame '@bot ' 帧状态机；bot_model link/seq/rev 规则；bot_router 手势表
  - 坑: capabilities 有 open_agent/open_usage；sparkline 允许 null 桶(NAN 占位)；hello 唯一 link_id=null/seq=0
- T06-T08 三屏固件实机运行（commit 59c5d32）
  - firmware/ 独立工程, BSP 3.0.1+LVGL 9.4.*; bot_core 直接入组件
  - 关键坑: bsp_display_lock 在 LVGL task 启动瞬间立即失败(INVALID_STATE 非超时) -> 用 -1 阻塞等
  - 触摸路径: lv_indev 原始坐标 10ms 泵 -> bot_gesture -> bot_route; LVGL 手势识别保持关闭
  - 每屏 SIM 徽标; WorkBuddy N/A; 亮度 28%
- 烧录流程已验证 3 次: flash_args 规范化 esptool 命令直调, 绕 idf.py 的 build/log shim 坑
- 待办: 用户目测三屏+手势(T14 收口); T10 USB 握手; T11 loopback; T15-T17 真实 adapter
