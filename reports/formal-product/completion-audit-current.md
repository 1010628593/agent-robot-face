# 当前完整目标验收核对

目标仍为用户批准的完整正式版计划，以及后续确认的用量设计03、同工具多周期环和简洁圆屏布局。未把目标缩减为通信修复或构建通过。

## 当前有效交付

固件 3.2.7，协议 v3，生产关闭自动回放和默认 SIM；Bridge 为独立 Python 服务，使用当前工作树。最近改动：用量网格/页签/费用层级、百分比边界、固定内部 DMA 缓冲、有界字节接收缓存、发送节流，以及总览只选择真实非空额度百分比。

## 对照原计划

| 要求 | 有效证据 | 当前结论 |
|---|---|---|
| 固定眼色、正常瞳孔、业务状态与互动分层、重要状态收束 | formal-face、touch-table 实现报告及早期用户实机反馈 | 已实现；后续共享触摸改动需按各自记录理解，不能由用量测试覆盖全部表情 |
| 98.7°安装参考、扶正、触摸朝向锁定、局部重绘 | imu-rotation-fix、touch-table；本轮未更改上述模型 | 保留；没有用新的物理旋转测试替代旧证据 |
| 中央互动、边缘导航、长按、冷却、无重复队列 | formal-face、touch-table、layout-audit/contacts | 已实现；最新用户主要反馈用量布局，仍待最终视觉/手感确认 |
| 真实四源自动观测；Codex Desktop 与 CLI 分别验证 | source-capabilities、source-probe-live、hardware-projection、当前 API | 有开始/工具/成功等真实证据；完整生命周期仍未通过 |
| WorkBuddy Desktop 失败/取消可靠分类，不准用 CLI/MCP 替代 | 真实 Desktop Stop 无 status/error/end_reason；incomplete 有歧义 | 阻塞。不能从通用 Stop 或 completed 标志伪造终态 |
| Hermes CLI 明确取消/失败事件 | 双 Ctrl-C exit130 未收到 on_session_end 的真实记录 | 取消仍未验证；不能把退出码当作既有观察器已捕获事件 |
| 四源等待必须明确输入/批准事件 | 当前 capabilities.waiting 均为 unverified | 保留缺口；不能以工具开始或超时推断等待 |
| 去重、迟到保护、持久账本、自动/锁定关注及 ACK | 现有 Bridge/PTY/API 集成及四源 USB 投影 | 实现及已覆盖路径通过；不证明未测来源生命周期 |
| Hook 最小合并、备份、回滚和独立环境/LaunchAgent | 原安装器集成与实际安装记录 | 已实现；未重新覆盖用户退出登录/登录的启动验收 |
| 单发送者、有限帧、UI 线程应用、版本不匹配、重连 | protocol-v3、现有集成；protocol-recovery | 3.2.7 字节接收缓冲通过短时实机压力回放；不等于无限期稳定 |
| 每批固件编译、烧录、真实运行；不以构建当帧率 | 各批 flash 日志、device-dma-fix、layout-audit、protocol-recovery | 已记录；真实提交时间与物理 FPS 严格区分 |
| 真实 Token/费用/额度，未知非零，来源/新鲜度 | usage API/collector/wire、live-layout 真数据 C 解码渲染 | 已实现部分覆盖；Cursor 空额度遮挡真实套餐百分比已修复 |
| 额度无官方渠道不得编造 | 当前 Codex/Hermes/WorkBuddy 部分渠道缺口 | 正确显示能力缺口，不能声称所有额度可用 |
| 设备总览、详情、多周期环、点击/滑动/逐层返回 | layout-audit 生产 LVGL、触摸回放、真实数据 live-layout | 实现有证据；用户对最新布局的实机视觉接受尚未确认 |
| 空态、过期、失败、首次连接、中文圆屏可读性 | edges/edges-after、旧正式 UI 报告、现有中文字体 | 已覆盖部分状态；不能宣称所有真实组合视觉验收通过 |
| 原生 Mac 菜单栏、真实数据与控制、诊断/恢复 | formal-mac、usage-dashboard 现有安装/构建记录 | 已实现；当前最新面板视觉/点击验收证据仍不足 |
| 并发、重复/迟到、来源退出、Bridge重启 | 集成与真实运行记录 | 已覆盖部分；静默任务活性仍有明确能力缺口 |
| 物理 USB 拔插、Mac 睡眠唤醒、登录后恢复 | 只有 close/reopen、暂停恢复、烧录恢复证据 | 未通过。不能将串口重开冒充物理拔插或睡眠唤醒 |
| 不新增前后端单元测试，保留模拟器为显式开发模式 | 本轮使用现有集成和独立 LVGL 回放；生产配置 OFF | 遵守；回放数据未注入生产统计 |

## 结论与后续入口

完整目标未达到完成条件。当前最直接的外部待办是用户指出最新圆屏布局仍不合理的位置，已有问题等待回复，不重复要求设计选择。另有真实外设/睡眠操作和最新 Mac 面板验收待执行；WorkBuddy / Hermes 的终态来源缺口不能降级为已完成。

当前已确认的布局、真实额度和接收丢块缺陷均有对应修复与验证，不为维持进度而继续无依据调整样式。没有将早期“显示与交互正常”或“卡顿明显改善”扩展为最新布局认可。

## 最新 Mac 面板读取复核

本轮确认 AgentRobotFace PID 17413 仍运行（约 2 小时），未因 UI 超时重启它。CUA 按应用名称和已确认安装路径读取均返回 timeoutReached；CUA 全局清单可读取，但没有提供此菜单栏应用的可操作面板。因此当前最新面板的视觉与点击验收仍被可见界面缺失阻塞，不能从进程存活推断通过。需要用户展开双眼菜单栏面板后才能继续检查；圆屏布局反馈的问题也仍未得到答复。

恢复任务后新增证据：Mac 面板已成功读取，完成 Codex/Cursor 切换、额度详情、来源说明和返回列表的实际 UI 验证。发现并局部修正多环状态卡底部说明压环，已构建安装；重启后的面板复核等待用户展开。详见 ../usage-dashboard/mac-live-review/acceptance.md。此进展取代此前“当前面板从未读取成功”的限制，但不代表修复后视觉已通过。

Mac 修复后实际复核更新：用户重新展开后，Cursor 多环说明间距确认正常，额度详情入口正常；返回状态卡出现 AX 有内容但截图为空的绘制问题，切换工具恢复，新增未通过项。见 mac-live-review/acceptance.md。

缺绘归因复查：同一进程再次 Cursor 详情返回画面完整；Hermes 截图间歇缺字，AX 数据存在；WorkBuddy 完整。现需区分实际应用缺绘与截图采集异常，尚未取得肉眼对照，不能把截图症状直接认定为 SwiftUI 根因。未增加推测性重绘补丁。

当前只读设备核查：固件3.2.7在线，串口现为 /dev/cu.usbmodem101，本次启动约40分钟，内部空闲49027字节、最大块31744字节、最低空闲10168字节；RX丢块/坏行/队列重置/超时重置/发送失败为0。Bridge累计1次Device not configured重连，记录早于当前采样约96分钟，不能称为本轮新故障或推断物理拔插验收通过。脱敏快照：../protocol-recovery/current-ui-review-health.json。当前脸页，不能以该读数证明用量手势压力稳定。
