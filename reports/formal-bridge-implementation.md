# Formal Bridge implementation handoff — 2026-09-07

Implemented files: bridge/src/bot_bridge/{runtime,adapters,usb,service,hook,cli}.py, tools/bridge, tools/bridge-integration.py, docs/{host-api,bridge-service}.md, dependency/version updates. Existing v1 fixture models retained; production transport is strict v2. No firmware or native app edits, no commits, no new unit tests.

## Completed and checked

Independent Python3.13.12 venv, SQLite metadata ledger, durable cursors, source/session/run/call dedup, per-call FSM, old-terminal/new-turn protection, terminal precedence, explicit waiting reasons, stable auto/pin, persisted pending attention, partial daily accounting and same-day cumulative deltas. Historical errors do not create fresh attention; missing stats remain null. API includes configured versus observed versus active, capability evidence per event kind, validated external coverage separately, privacy-safe diagnostics.

Adapters: Codex native Desktop/CLI rollouts with source+originator filtering, child exclusion, native failure/error/abort semantics and token metadata. Cursor installed official hooks plus actual native hook-log fallback, stable generation/tool IDs and cancellation precedence. Hermes CLI-only SQLite and native shell hooks; same-session canonical user identity mapping is persisted and ambiguous timing remains pending instead of guessing. WorkBuddy Desktop JSONL parent graph, explicit isMeta exclusion, real tool/success and ambiguous incomplete as unknown. Candidate native observer is diagnostics-only until root validates actual Desktop delivery/status fields. No agent process is substituted for WorkBuddy.

Single serial owner opens with DTR/RTS false, exclusive lock, protocol2 handshake, strict fields/duplicates/enums/size/link/sequence/action validation, atomic revision ACK, full reconnect snapshots, ping timeout, explicit mismatch, paused release/resume, separate @diag collection. No physical serial port was opened by this worker; PTY only.

Loopback17940 API with Host/Origin guard and private bearer token, native app contract, all control paths, installation/repair/doctor/uninstall and flash pause-finally wrapper. Minimal official JSON/YAML hook merge preserves existing integrations. Only fixed observer commands are stored; no credentials copied into code/install commands. Private backups and exact owned-entry manifests. Hermes native first-use consent remains enabled. User LaunchAgent installer supplied but actual installation deferred to controller.

## Verification evidence

- `PYTHONPATH=bridge/src .venv-bridge/bin/python -m compileall -q bridge/src` — PASS.
- `.venv-bridge/bin/python tools/bridge-integration.py` —12 scenario groups PASS: late old terminal, concurrent calls/approval, out-of-order call endings, terminal duplicate/retry safety, auto ties/pin, historical attention, usage/midnight baselines, ledger restart, WorkBuddy meta, native schema boundary, actual POSIX PTY v2 handshake/ACK/diag, idempotent installer and owned uninstall.
- Same runner real subprocess HTTP checks — PASS: local endpoint, unobserved null counts,401 missing token,403 browser Origin,409 revision conflict, pin, pause/release, resume. Sandbox initially denied bind17941; approved escalation completed verification. Inherited dead proxy initially intercepted urllib local requests; local API clients now explicitly use empty ProxyHandler, without changing any system/source proxy settings.
- `.venv-bridge/bin/python /tmp/bridge-live-reader-check.py` — read-only actual sources in an ephemeral private store, no device: all4 observed, no reader errors,200 bounded source files, maximum poll83.02ms over35polls. Actual metadata detected Codex start/tool/done/error/interrupt, Cursor start/tool/done/aborted, Hermes CLI start/paired tool/done, WorkBuddy human start/tools/done/incomplete. This is parser/replay evidence, not new source task execution by Bridge.
- `.venv-bridge/bin/python /tmp/bridge-yaml-check.py` — PASS on private temporary copy of actual Hermes config. Ruby YAML parsed before/after; every unrelated field and preexisting hook preserved; repair byte-idempotent; uninstall returned identical parsed config. Temporary copies deleted, no secret output.
- `tools/bridge doctor` verified Python3.13.12, four source config directories present, own LaunchAgent not yet installed at worker check time.

## Controller installation and remaining acceptance

Run `tools/bridge install` with authorized user-level filesystem/launchctl access. Then `tools/bridge doctor`. Port17940, token `~/.local/share/agent-robot-face/control.token`. Complete real serial/service reconnect and flash wrapper acceptance; native app uses docs/host-api.md. Validate newly installed source hook receipt after source reload and exact Hermes observer trust; installer does not globally auto-accept hooks. Root should inspect bounded `workbuddy-candidates.jsonl` during actual WorkBuddy tasks to establish whether native Stop offers a reliable cancellation/failure enum. Candidate diagnostics never change business state. Source waiting/official quotas remain unverified; do not claim full four-source lifecycle acceptance until actual tests prove them.

Known boundedness/coverage:200 recent files maximum,2-day source window,80ms incremental read budget plus discovery/stat overhead, bounded256KiB Cursor parse chunks/100JSONL records. Counts remain partial during/after backfill; first usage sample and first sample after midnight are baselines with unknown daily amount. Long silent active tasks become stale with an explicit liveness gap; time never creates a failure. Known terminal states remain until new source state. Existing venv depends on its trusted base interpreter path, so removal of that interpreter requires recreating the isolated venv. Official quota UI stays unavailable.
